<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical">

    <!-- Header bar with back button and title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primaryDark"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="@dimen/settings_header_padding">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="@dimen/settings_header_icon_size"
            android:layout_height="@dimen/settings_header_icon_size"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/back"
            android:src="@drawable/ic_back"
            android:tint="@color/textPrimary" />

        <TextView
            android:id="@+id/settings_title"
            style="@style/SettingsHeaderStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/tile_spacing"
            android:text="@string/settings" />
    </LinearLayout>

    <!-- Container for PreferenceFragmentCompat content -->
    <FrameLayout
        android:id="@+id/preferences_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/background" />

</LinearLayout>
